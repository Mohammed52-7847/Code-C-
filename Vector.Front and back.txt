#include <iostream>
#include <vector>
using namespace std;
int main()
{
	vector <int> vNumbers;
	vNumbers.push_back(10);
	vNumbers.push_back(20);
	vNumbers.push_back(30);
	vNumbers.push_back(40);
	vNumbers.push_back(50);
	vNumbers.push_back(100);
	vNumbers.push_back(100);
	vNumbers.push_back(100);
	vNumbers.push_back(100);
	vNumbers.push_back(5);

	vNumbers.clear();

	//cout << "The Front Elements: " << vNumbers.front() << endl;
	//cout << "The Back Elements: " << vNumbers.back() << endl;
	cout << "The Capacity Elements: " << vNumbers.capacity() << endl;
	cout << "The Size Elements: " << (vNumbers.size() > 0) << endl;
	cout << "The Empty Elements: " << (!vNumbers.empty()) << endl;

	for (int& Number : vNumbers)
	{
		cout << Number << endl;
		cout << endl;
	}
	cout << endl;

	return 0;
}
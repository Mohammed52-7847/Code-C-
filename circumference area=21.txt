#include <iostream>
using namespace std;

int main ()
{
    int l;
    cout << "please enter your number-L = ";
    cin >> l;
    const float pi = 3.14;
    double circumference_area = (l * l) / (4 * pi);
    cout << "The circumference area is = " << circumference_area << "." << endl;

return 0;
}
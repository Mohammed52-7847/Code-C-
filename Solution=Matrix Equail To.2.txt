#include <iostream>
#include <string>
#include <iomanip>
#include <cstdio>
using namespace std;
int RandomNumber(int From, int To)
{
	int randNum = rand() % (To - From + 1) + From;
	return randNum;
}
void FillMatrix(int arr[3][3], short Rows, short Cols)
{
	for (short i = 0; i < Rows; i++)
	{
		for (short j = 0; j < Cols; j++)
		{
			arr[i][j] = RandomNumber(1,10);
		}
	}
}
void CopyFillMatrix(int arr[3][3], int Destension[3][3], short Rows, short Cols)
{
	for (short i = 0; i < Rows; i++)
	{
		for (short j = 0; j < Cols; j++)
		{
			Destension[i][j] = arr[i][j];
		}
	}
}
void PrintMatrix(int arr[3][3], short Rows, short Cols)
{
	for (short i = 0; i < Rows; i++)
	{
		for (short j = 0; j < Cols; j++)
		{
			printf(" %0*d  ", 2, arr[i][j]);
		}
		cout << endl;
	}
}
int SumMatrix(int arr[3][3], short Rows, short Cols)
{
	short Sum = 0;
	for (short i = 0; i < Rows; i++)
	{
		for (short j = 0; j < Cols; j++)
		{
			Sum += arr[i][j];
		}
	}
	return Sum;
}
//bool EquailM(int arr[3][3], int Destension[3][3], short Rows, short Cols)
//{
//	return (SumMatrix(arr, 3, 3) == SumMatrix(Destension, 3, 3));
//}
//void EquailMatrix(int arr[3][3], int Destension[3][3], short Rows, short Cols)
//{
//	if (EquailM( arr, Destension, 3,  3))
//	{
//		cout << "  Equail to\n";
//	}
//	else
//	{
//		cout << "  Not Equail to\n";
//	}
//}
void EquailMatrix(int arr[3][3], int Destension[3][3], short Rows, short Cols)
{
	if (SumMatrix(arr, 3, 3) == SumMatrix(Destension, 3, 3))
	{
		cout << "  Equail to\n";
	}
	else
	{
		cout << "  Not Equail to\n";
	}
}
int main()
{
	srand((unsigned)time(NULL));

	int arr[3][3], arr2[3][3];
	FillMatrix(arr, 3, 3);


	cout << "\nMatrix1:\n";
	PrintMatrix(arr, 3, 3);

	cout << "\nSum Matrix1:\n";
	cout << "  " << SumMatrix(arr, 3, 3) << endl;

	FillMatrix( arr2, 3, 3);
	//CopyFillMatrix(arr, arr2, 3, 3);

	cout << "\nMatrix2:\n";
	PrintMatrix(arr2, 3, 3);

	cout << "\nSum Matrix1:\n";
	cout << "  " << SumMatrix(arr2, 3, 3) << endl;

	cout << "\n\n";

	EquailMatrix(arr, arr2, 3, 3);

	//EquailMatrix(arr, arr2, 3, 3);

	/*if (SumMatrix(arr, 3, 3) == SumMatrix(arr2, 3, 3))
	{
		cout << "  Equail to\n";
	}
	else
	{
		cout << "  Not Equail to\n";
	}*/

	system("pause>0");
}
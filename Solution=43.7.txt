#include <iostream>
#include <string>
#include <vector>
using namespace std;
string ReadString()
{
	string S1;
	cout << "Please Enter Your String?\n";
	getline(cin, S1);
	return S1;
}
string UpperrFirstWordS1(string S1)
{
	bool isFirstLetter2 = true;
	for (short i = 0; i < S1.length(); i++)
	{
		if (S1[i] != ' ' && isFirstLetter2)
		{
			S1[i] = toupper(S1[i]);
		}
		isFirstLetter2 = (S1[i] == ' ' ? true : false);
	}
	return S1;
}
string UpperrFirstWord(string Name2)
{
	bool isFirstLetter = true;
	for (short i = 0; i < Name2.length(); i++)
	{
		if (Name2[i] != ' '&& isFirstLetter)
		{
			Name2[i] = toupper(Name2[i]);
		}
		isFirstLetter = (Name2[i] == ' ' ? true : false);
	}
	return Name2;
}
string ReplaceWords(string S1, string Name1, string Name2)
{
	short Pos = S1.find(Name1);
	
	S1.erase(Pos, Name1.length());
	S1.insert(Pos, Name2);
	
	return S1;
}
int main()
{
	string S1 = "mohammed Helmi Ahmed Baghdadi";
	cout << S1;
	S1 = UpperrFirstWordS1(S1);
	cout << "\n" << S1;

	string Name1 = "Mohammed";
	string Name2 = "reham";
	Name2 = UpperrFirstWord(Name2);




	cout << "\n\n" << ReplaceWords(S1, Name1, Name2);

	system("pause>0");
}.7
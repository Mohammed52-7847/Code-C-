#include <iostream>
#include <string>
using namespace std;
int PrintNumbers(int N, int M)
{
	if (M == 0)
	{
		return 1;
	}
	else
	{
		return N * PrintNumbers(N, M - 1);
	}
}

int main()
{
	cout << PrintNumbers(2, 4) << endl;

	return 0;
}